export interface RegisterFormValues {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
    majority: boolean;
}

export interface LoginFormValues {
    email: string;
    password: string;
}
