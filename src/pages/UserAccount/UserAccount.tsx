import React from 'react';
import {AccountContainer, PageContainer} from './UserAccount.styled';

export const UserAccount: React.FC = () => {
    return (
        <PageContainer>
            <AccountContainer>User Account</AccountContainer>
        </PageContainer>
    );
};
